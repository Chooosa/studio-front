import styled from 'styled-components';

export const MapContainer = styled.div`
   border-radius: 12px;
   display: flex;
   justify-content: center;
   align-items: center;

   .mapContainer {
      /* width: 466px;
      height: 291px; */
      border-radius: 12px;
      overflow: hidden;
   }
`